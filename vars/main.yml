---
# vars file for ansible-role-pass
# vars file for openssh
_pass_packages:
  default:
    - git
    - pwgen
    - pass
    # - qtpass
  Archlinux:
    - git
    - pwgen
    - pass
    # - qtpass
  openSUSE Leap:
    - git
    - pwgen
    - pass
    # - qtpass

pass_packages: "{{ _pass_packages[ansible_distribution] | default(_pass_packages['default']) }}"

